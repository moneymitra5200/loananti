import { NextRequest, NextResponse } from 'next/server';import { db } from '@/lib/db';// ─── Customer context from DB ─────────────────────────────────────────────────async function getCustomerContext(customerId: string) {  // Sequential queries to respect Hostinger's DB connection limit (3 max)  // Wrapped in a 4s timeout so a slow DB never hangs the chat response  const timeout = <T>(promise: Promise<T>, ms = 4000): Promise<T | null> =>    Promise.race([promise, new Promise<null>(r => setTimeout(() => r(null), ms))]);  try {    const customer = await timeout(      db.user.findUnique({        where: { id: customerId },        select: { name: true, phone: true, email: true },      })    );    const loans = await timeout(      db.loanApplication.findMany({        where: { customerId, status: { in: ['DISBURSED', 'ACTIVE', 'ACTIVE_INTEREST_ONLY'] } },        include: {          emiSchedules: { orderBy: { installmentNumber: 'asc' } },          payments: { orderBy: { createdAt: 'desc' }, take: 5 },          company: { select: { name: true } },        },        take: 5,      })    ) ?? [];    const allLoans = await timeout(      db.loanApplication.findMany({        where: { customerId },        select: { id: true, applicationNo: true, status: true, loanAmount: true, disbursedAt: true, loanType: true },        orderBy: { createdAt: 'desc' },        take: 10,      })    ) ?? [];    const loanContexts = loans.map((loan: any) => {      const emis = loan.emiSchedules || [];      const pending = emis.filter((e: any) => e.paymentStatus === 'PENDING');      const overdue = emis.filter((e: any) => e.paymentStatus === 'OVERDUE');      const paid = emis.filter((e: any) => ['PAID', 'INTEREST_ONLY_PAID'].includes(e.paymentStatus));      const nextEmi = [...pending].sort((a: any, b: any) => new Date(a.dueDate).getTime() - new Date(b.dueDate).getTime())[0];      const overdueAmount = overdue.reduce((s: number, e: any) => s + Number(e.totalAmount || 0), 0);      const paidAmount = paid.reduce((s: number, e: any) => s + Number(e.paidAmount || 0), 0);      const outstandingPrincipal = emis.find((e: any) => e.paymentStatus !== 'PAID')?.outstandingPrincipal || 0;      const daysUntilNextEmi = nextEmi ? Math.ceil((new Date(nextEmi.dueDate).getTime() - Date.now()) / 86400000) : null;      return {        applicationNo: loan.applicationNo,        loanAmount: Number(loan.loanAmount),        company: loan.company?.name || 'MoneyMitra',        status: loan.status,        loanType: loan.loanType || 'PERSONAL',        totalEMIs: emis.length,        paidEMIs: paid.length,        pendingEMIs: pending.length,        overdueEMIs: overdue.length,        overdueAmount,        paidAmount,        outstandingPrincipal: Number(outstandingPrincipal),        daysUntilNextEmi,        nextEmi: nextEmi ? {          installmentNumber: nextEmi.installmentNumber,          dueDate: new Date(nextEmi.dueDate).toLocaleDateString('en-IN', { day: '2-digit', month: 'short', year: 'numeric' }),          amount: Number(nextEmi.totalAmount),          status: nextEmi.paymentStatus,        } : null,        recentPayments: (loan.payments || []).map((p: any) => ({          amount: Number(p.amount),          date: new Date(p.createdAt).toLocaleDateString('en-IN'),          mode: p.paymentMode,        })),      };    });    return { customerName: customer?.name || '', customerPhone: customer?.phone || '', loanContexts, allLoans };  } catch {    return { customerName: '', customerPhone: '', loanContexts: [], allLoans: [] };  }}// ─── Smart suggestions based on context ──────────────────────────────────────function getSmartSuggestions(loans: ReturnType<typeof getCustomerContext> extends Promise<infer T> ? any : any): string[] {  const suggestions: string[] = [];  if (!loans || loans.length === 0) {    suggestions.push('📝 How do I apply for a loan?');    suggestions.push('💼 What loan products are available?');    suggestions.push('📋 What documents do I need?');    return suggestions;  }  const hasOverdue = loans.some((l: any) => l.overdueEMIs > 0);  const hasUpcoming = loans.some((l: any) => l.daysUntilNextEmi !== null && l.daysUntilNextEmi >= 0 && l.daysUntilNextEmi <= 7);  const nearlyPaid = loans.some((l: any) => l.paidEMIs > 0 && l.pendingEMIs <= 3);  if (hasOverdue) suggestions.push('⚠️ How to clear my overdue EMIs?');  if (hasUpcoming) suggestions.push('📅 When is my next EMI due?');  if (nearlyPaid) suggestions.push('🏁 How to close my loan early?');  suggestions.push('💰 What is my outstanding balance?');  suggestions.push('📄 Show my payment history');  if (loans.every((l: any) => l.overdueEMIs === 0)) suggestions.push('📈 Can I get a top-up or new loan?');  return suggestions.slice(0, 4);}// ─── Response engine ──────────────────────────────────────────────────────────function buildResponse(  message: string,  name: string,  ctx: { loanContexts: any[]; allLoans: any[] }): { response: string; intent: string; suggestions: string[] } {  const msg = message.toLowerCase().trim();  const loans = ctx.loanContexts;  const firstName = name.split(' ')[0] || 'there';  const inr = (n: number) => `₹${n.toLocaleString('en-IN')}`;  const sugg = getSmartSuggestions(loans);  // ── Greeting ──────────────────────────────────────────────────────────────  if (/^(hi|hello|hey|namaste|hii|helo|good morning|good evening|good afternoon|hy)\b/.test(msg)) {    const hasOverdue = loans.some(l => l.overdueEMIs > 0);    const nextDue = loans.find(l => l.daysUntilNextEmi !== null && l.daysUntilNextEmi >= 0);    let reply = `Hello ${firstName}! 😊 Welcome to MoneyMitra — I'm **Mitra**, your personal loan assistant.\n\n`;    if (hasOverdue) {      reply += `⚠️ **Heads up!** You have overdue EMIs that need attention. Let's sort that out quickly.\n\n`;    } else if (nextDue && nextDue.daysUntilNextEmi !== null && nextDue.daysUntilNextEmi <= 5) {      reply += `📅 **Reminder:** Your next EMI of ${inr(nextDue.nextEmi?.amount || 0)} is due in **${nextDue.daysUntilNextEmi} day(s)**.\n\n`;    } else if (loans.length > 0) {      reply += `✅ You have **${loans.length} active loan(s)** and everything looks good!\n\n`;    }    reply += `How can I help you today? Here are some things I can do:\n• Check your EMI status & dues\n• Show outstanding balance\n• Guide you on payments\n• Suggest loans you qualify for\n• Help with foreclosure & top-ups\n\nJust ask me anything! 🙏`;    return { response: reply, intent: 'GREETING', suggestions: sugg };  }  // ── EMI / Due Dates ───────────────────────────────────────────────────────  if (/emi|due|next pay|installment|kab hai|payment date|kab bharna|kitni emi/.test(msg)) {    if (loans.length === 0) {      return { response: `You don't have any active loans right now, ${firstName}! 😊\n\nIf you'd like to apply for a loan, I can guide you through the process. Want to know what you might be eligible for?`, intent: 'EMI_STATUS', suggestions: sugg };    }    let reply = `📅 **Your EMI Status, ${firstName}:**\n\n`;    loans.forEach((loan, i) => {      reply += `**Loan ${i + 1} — ${loan.applicationNo}** (${loan.company})\n`;      reply += `• Loan Amount: ${inr(loan.loanAmount)}\n`;      reply += `• Progress: ${loan.paidEMIs} of ${loan.totalEMIs} EMIs paid `;      reply += `(${Math.round((loan.paidEMIs / loan.totalEMIs) * 100)}% complete)\n`;      if (loan.nextEmi) {        const isOverdue = loan.nextEmi.status === 'OVERDUE';        const daysText = loan.daysUntilNextEmi !== null          ? (loan.daysUntilNextEmi < 0 ? `**${Math.abs(loan.daysUntilNextEmi)} days overdue**` : `in ${loan.daysUntilNextEmi} day(s)`)          : '';        reply += `• ${isOverdue ? '🔴 OVERDUE' : '📅 Next EMI'}: ${inr(loan.nextEmi.amount)} on **${loan.nextEmi.dueDate}** ${daysText}\n`;      } else if (loan.paidEMIs === loan.totalEMIs) {        reply += `• ✅ All EMIs completed — Loan fully paid off!\n`;      }      if (loan.overdueEMIs > 0) {        reply += `• ⚠️ ${loan.overdueEMIs} overdue EMI(s) — Total: ${inr(loan.overdueAmount)}\n`;        reply += `  → Please clear ASAP to avoid penalty charges.\n`;      }      reply += '\n';    });    reply += `💡 **Tip:** Pay a few days early to avoid last-minute issues and keep your credit record clean!`;    return { response: reply, intent: 'EMI_STATUS', suggestions: sugg };  }  // ── Outstanding Balance ───────────────────────────────────────────────────  if (/balance|outstanding|remaining|kitna bacha|how much left|total due|baki/.test(msg)) {    if (loans.length === 0) {      return { response: `You have no active loans right now, ${firstName}. Your slate is clean! 😊\n\nWant to explore new loan options?`, intent: 'LOAN_BALANCE', suggestions: sugg };    }    let reply = `💰 **Your Outstanding Balance, ${firstName}:**\n\n`;    let grandTotal = 0;    loans.forEach((loan, i) => {      reply += `**${i + 1}. ${loan.applicationNo}**\n`;      reply += `• Outstanding Principal: **${inr(loan.outstandingPrincipal)}**\n`;      reply += `• Total Paid So Far: ${inr(loan.paidAmount)}\n`;      reply += `• Remaining EMIs: ${loan.pendingEMIs}\n\n`;      grandTotal += loan.outstandingPrincipal;    });    if (loans.length > 1) {      reply += `**📊 Grand Total Outstanding: ${inr(grandTotal)}**\n\n`;    }    reply += `💡 **Hint:** You can save on interest by paying extra towards principal. Ask me about "loan foreclosure" to see how much you'd save!`;    return { response: reply, intent: 'LOAN_BALANCE', suggestions: sugg };  }  // ── Overdue / Penalty ─────────────────────────────────────────────────────  if (/overdue|penalty|late|fine|default|penal|charge/.test(msg)) {    const overdueLoans = loans.filter(l => l.overdueEMIs > 0);    if (overdueLoans.length === 0) {      return {        response: `✅ Great news, ${firstName}! You have **no overdue EMIs** at all.\n\nYou're a responsible borrower — this reflects positively on your credit profile. Keep it up! 🌟\n\n💡 **Hint:** Good repayment history makes you eligible for **higher loan amounts** and **lower interest rates** in the future!`,        intent: 'PENALTY_INFO', suggestions: sugg      };    }    let reply = `⚠️ **Overdue Alert for ${firstName}:**\n\n`;    overdueLoans.forEach(loan => {      reply += `📌 **${loan.applicationNo}**\n`;      reply += `• Overdue EMIs: ${loan.overdueEMIs}\n`;      reply += `• Overdue Amount: **${inr(loan.overdueAmount)}**\n\n`;    });    reply += `⚡ **What to do now:**\n1. Contact your cashier or branch immediately\n2. Make the payment in cash, UPI, or bank transfer\n3. Penalties increase every day — act today!\n\n📞 Ask me to "raise a support ticket" if you need help.`;    return { response: reply, intent: 'PENALTY_INFO', suggestions: sugg };  }  // ── Payment History ───────────────────────────────────────────────────────  if (/payment history|paid|receipt|transaction|history|purana payment/.test(msg)) {    if (loans.length === 0) {      return { response: `No payment history yet, ${firstName}. You don't have any active loans.\n\nWould you like to apply for one? I can tell you what you might qualify for! 😊`, intent: 'PAYMENT_HISTORY', suggestions: sugg };    }    let reply = `📄 **Recent Payments, ${firstName}:**\n\n`;    let hasAny = false;    loans.forEach(loan => {      if (loan.recentPayments.length > 0) {        hasAny = true;        reply += `**${loan.applicationNo}** (${loan.paidEMIs}/${loan.totalEMIs} EMIs done):\n`;        loan.recentPayments.forEach((p: any) => {          reply += `  ✅ ${inr(p.amount)} — ${p.date} via ${p.mode}\n`;        });        reply += '\n';      }    });    if (!hasAny) reply += `No recent payments recorded yet.\n`;    reply += `📊 **Summary:**\n`;    loans.forEach(loan => {      reply += `• ${loan.applicationNo}: ${inr(loan.paidAmount)} paid (${loan.paidEMIs} EMIs)\n`;    });    return { response: reply, intent: 'PAYMENT_HISTORY', suggestions: sugg };  }  // ── Loan Status ───────────────────────────────────────────────────────────  if (/loan status|my loan|application|loan detail|sanction|approved|rejected/.test(msg)) {    if (ctx.allLoans.length === 0) {      return { response: `You don't have any loan applications yet, ${firstName}.\n\n🚀 Ready to apply? I can guide you through the entire process — documents needed, eligibility, and more. Just ask!`, intent: 'LOAN_STATUS', suggestions: sugg };    }    const icons: Record<string, string> = { DISBURSED: '✅', ACTIVE: '🟢', CLOSED: '🏁', OVERDUE: '🔴', PENDING: '⏳', SUBMITTED: '📤', APPROVED: '👍', REJECTED_BY_SA: '❌', FINAL_APPROVED: '✅', CUSTOMER_SESSION_APPROVED: '🔄' };    let reply = `📋 **Your Loan Applications, ${firstName}:**\n\n`;    ctx.allLoans.forEach((loan: any) => {      reply += `${icons[loan.status] || '📄'} **${loan.applicationNo}** — ${inr(Number(loan.loanAmount))} — **${loan.status.replace(/_/g, ' ')}**\n`;    });    reply += `\n💡 **Hint:** Ask me about any specific loan for detailed EMI schedule, payment options, or foreclosure quote!`;    return { response: reply, intent: 'LOAN_STATUS', suggestions: sugg };  }  // ── Foreclosure / Close Loan ──────────────────────────────────────────────  if (/foreclose|close loan|prepay|full payment|close my loan|banda karna/.test(msg)) {    if (loans.length === 0) {      return { response: `You don't have any active loans to foreclose right now, ${firstName}. 😊\n\nWant to apply for a new loan instead?`, intent: 'FORECLOSURE', suggestions: sugg };    }    let reply = `🏁 **Loan Foreclosure Guide, ${firstName}:**\n\nClosing your loan early saves you interest! Here's what you owe:\n\n`;    loans.forEach(loan => {      const saved = Math.round(loan.pendingEMIs * (loan.nextEmi?.amount || 0) - loan.outstandingPrincipal);      reply += `**${loan.applicationNo}**\n• Outstanding: **${inr(loan.outstandingPrincipal)}**\n• Remaining EMIs: ${loan.pendingEMIs}\n`;      if (saved > 0) reply += `• 💡 You'd save approx. **${inr(saved)}** in interest by closing now!\n`;      reply += '\n';    });    reply += `**Steps to foreclose:**\n1. Visit your branch or contact your cashier\n2. Ask for the foreclosure statement\n3. Pay the outstanding principal\n4. Get your **No Dues Certificate** 🎉\n\n⚠️ Some loans may have a foreclosure fee — ask your cashier.`;    return { response: reply, intent: 'FORECLOSURE', suggestions: sugg };  }  // ── New Loan / Apply / Suggest ────────────────────────────────────────────  if (/apply|new loan|loan lena|suggest|eligib|qualify|top.?up|top up|loan chahiye|loan milega/.test(msg)) {    const goodHistory = loans.length > 0 && loans.every(l => l.overdueEMIs === 0);    let reply = `📝 **Loan Options for You, ${firstName}:**\n\n`;    if (goodHistory) {      reply += `🌟 Based on your **excellent repayment record**, you're likely eligible for **preferential rates!**\n\n`;    }    reply += `💼 **Personal Loan** — ₹50K to ₹10L | 14–24% p.a. | 6–60 months\n`;    reply += `   Best for: medical emergencies, travel, education, weddings\n\n`;    reply += `🏢 **Business Loan** — ₹1L to ₹50L | 12–20% p.a. | 12–84 months\n`;    reply += `   Best for: working capital, expansion, equipment\n\n`;    reply += `🥇 **Gold Loan** — Up to 75% of gold value | 8–16% p.a. | Instant!\n`;    reply += `   Best for: quick cash needs with gold as collateral\n\n`;    reply += `🚗 **Vehicle Loan** — ₹50K to ₹20L | 10–18% p.a. | 12–60 months\n\n`;    if (loans.length > 0) {      const activeLoan = loans[0];      if (activeLoan.paidEMIs >= Math.floor(activeLoan.totalEMIs / 2)) {        reply += `✨ **Top-Up Loan:** Since you've paid over 50% of your loan, you may qualify for a **top-up on ${activeLoan.applicationNo}**! Ask your agent.\n\n`;      }    }    reply += `**Documents Needed:**\n• PAN + Aadhaar\n• Income proof (salary slip / ITR)\n• 6-month bank statement\n• Address proof\n\n`;    reply += `**How to Apply:** Contact your MoneyMitra agent or visit the branch. Approval in 24–48 hours! 🚀`;    return { response: reply, intent: 'LOAN_SUGGESTION', suggestions: sugg };  }  // ── How to Pay ────────────────────────────────────────────────────────────  if (/how to pay|payment mode|pay online|pay emi|kaise pay|payment karna|upi|online pay/.test(msg)) {    return {      response: `💳 **How to Pay Your EMI, ${firstName}:**\n\n**Through the App/Dashboard:**\n1. Go to "My Loans"\n2. Select your loan → "Pay EMI"\n3. Choose payment mode → Done! ✅\n\n**Payment Modes Accepted:**\n• 💵 Cash (at branch)\n• 📱 UPI — Google Pay, PhonePe, Paytm\n• 🏦 Net Banking / NEFT / RTGS\n• 💳 Debit/Credit Card\n• 📝 Cheque\n\n**At Branch:**\nWalk in with cash or cheque — your cashier will record it instantly.\n\n💡 **Tip:** Always save your payment receipt for reference!`,      intent: 'PAYMENT_HELP', suggestions: sugg    };  }  // ── Interest Rates ────────────────────────────────────────────────────────  if (/interest|rate|byaj|percent|sood|emi amount|how much emi/.test(msg)) {    const goodPayer = loans.length > 0 && loans.every(l => l.overdueEMIs === 0);    let reply = `📊 **Interest Rates at MoneyMitra, ${firstName}:**\n\n`;    reply += `| Loan Type | Rate (p.a.) |\n|-----------|-------------|\n`;    reply += `| Personal Loan | 14% – 24% |\n`;    reply += `| Business Loan | 12% – 20% |\n`;    reply += `| Gold Loan | 8% – 16% |\n`;    reply += `| Vehicle Loan | 10% – 18% |\n\n`;    reply += `**Your rate depends on:** credit history, loan amount, tenure & income.\n\n`;    if (goodPayer) reply += `🌟 **Great news!** Your clean repayment record puts you in line for **lower rates** on your next loan!\n\n`;    reply += `💡 Use our EMI calculator to estimate your monthly payment before applying.`;    return { response: reply, intent: 'INTEREST_RATES', suggestions: sugg };  }  // ── Support ───────────────────────────────────────────────────────────────  if (/support|help|contact|human|agent|problem|complaint|issue|manager|escalate/.test(msg)) {    return {      response: `📞 **I'm here, ${firstName}! Let me connect you:**\n\n🎫 **Raise a Support Ticket:**\nDashboard → Support → New Ticket\nOur team responds within 2–4 hours.\n\n📱 **Talk to your Agent:**\nContact your assigned MoneyMitra agent directly.\n\n🏢 **Branch Visit:**\nVisit during business hours: **9 AM – 6 PM, Mon–Sat**\n\n💬 **I'm available 24/7** for instant answers!\n\nWhat's the issue? Tell me and I'll either solve it or get the right person involved. 🙏`,      intent: 'SUPPORT', suggestions: sugg    };  }  // ── Thanks ────────────────────────────────────────────────────────────────  if (/thank|thanks|shukriya|dhanyavad|great|helpful|good|nice|perfect|bahut acha/.test(msg)) {    return {      response: `You're very welcome, ${firstName}! 😊🙏\n\nIt's my pleasure to assist you. I'm here anytime you need — whether it's checking your EMI, understanding your loan, or just a question.\n\nHave a wonderful day! Take care. 🌟`,      intent: 'THANKS', suggestions: sugg    };  }  // ── Default with smart hints ──────────────────────────────────────────────  const hasLoans = loans.length > 0;  let reply = `I'm Mitra, your MoneyMitra assistant! 😊 Let me help you with that.\n\n`;  if (hasLoans) {    const upcoming = loans.find(l => l.daysUntilNextEmi !== null && l.daysUntilNextEmi >= 0 && l.daysUntilNextEmi <= 5);    const overdueLoan = loans.find(l => l.overdueEMIs > 0);    if (overdueLoan) reply += `⚠️ **Quick Note:** You have overdue EMIs on ${overdueLoan.applicationNo} — clear them soon to avoid penalties!\n\n`;    else if (upcoming) reply += `📅 **Reminder:** EMI of ${inr(upcoming.nextEmi?.amount || 0)} due in ${upcoming.daysUntilNextEmi} day(s).\n\n`;  }  reply += `Here's what I can help with:\n📅 EMI status & due dates\n💰 Outstanding balance\n📄 Payment history\n📝 New loan eligibility\n🏁 Loan foreclosure\n💳 Payment methods\n📞 Support & escalation\n\nTry asking: *"When is my next EMI?"* or *"What is my balance?"* 😊`;  return { response: reply, intent: 'GENERAL', suggestions: sugg };}// ─── POST ─────────────────────────────────────────────────────────────────────export async function POST(request: NextRequest) {  try {    const body = await request.json();    const { customerId, sessionId, message, customerName } = body;    if (!customerId || !sessionId || !message) {      return NextResponse.json({ error: 'Missing required fields' }, { status: 400 });    }    // Get customer context (sequential, timeout-protected)    const ctx = await getCustomerContext(customerId);    const resolvedName = customerName || ctx.customerName || '';    const { response: aiResponse, intent, suggestions } = buildResponse(message, resolvedName, ctx);    // Save to DB as true fire-and-forget — NEVER blocks the response    setImmediate(() => {      db.aIChatHistory.create({        data: { customerId, sessionId, userMessage: message, aiResponse, intent },      }).catch(() => { /* non-critical — history save failure doesn't affect UX */ });    });    return NextResponse.json({ success: true, response: aiResponse, intent, suggestions });  } catch (error) {    // Even on catastrophic failure, return a helpful fallback response    // rather than a 500 that shows an error in the chat UI.    const fallback = "Hello! I'm Mitra, your MoneyMitra assistant. I can help with your EMI status, loan balance, payment history, and more. What would you like to know? 😊";    return NextResponse.json({      success: true,      response: fallback,      intent: 'GREETING',      suggestions: ['📅 EMI Due Dates', '💰 Outstanding Balance', '📄 Payment History', '📞 Contact Support'],    });  }}// ─── GET ──────────────────────────────────────────────────────────────────────export async function GET(request: NextRequest) {  try {    const { searchParams } = new URL(request.url);    const customerId = searchParams.get('customerId');    const sessionId = searchParams.get('sessionId');    if (!customerId) return NextResponse.json({ error: 'customerId is required' }, { status: 400 });    try {      const history = await db.aIChatHistory.findMany({        where: { customerId, ...(sessionId ? { sessionId } : {}) },        orderBy: { createdAt: 'asc' },        take: sessionId ? undefined : 50,      });      return NextResponse.json({ success: true, history });    } catch {      return NextResponse.json({ success: true, history: [] });    }  } catch (error) {    return NextResponse.json({ error: 'Failed to fetch chat history' }, { status: 500 });  }}